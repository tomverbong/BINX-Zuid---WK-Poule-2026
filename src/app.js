const data=window.POULE_DATA||{};const els={matchesContainer:document.getElementById('matchesContainer'),bonusContainer:document.getElementById('bonusContainer'),standBody:document.querySelector('#standTable tbody'),matchesView:document.getElementById('matchesView'),bonusView:document.getElementById('bonusView')};function init(){renderStand();renderMatches();renderBonus();bindToggle()}function bindToggle(){document.querySelectorAll('.toggle-btn').forEach(btn=>{btn.addEventListener('click',()=>{const view=btn.dataset.view;document.querySelectorAll('.toggle-btn').forEach(b=>b.classList.toggle('active',b===btn));els.matchesView.classList.toggle('active-view',view==='matches');els.bonusView.classList.toggle('active-view',view==='bonus')})})}function renderStand(){const rows=[...(data.stand||[])].sort((a,b)=>Number(b.totaal)-Number(a.totaal)||Number(b.punten_wedstrijden)-Number(a.punten_wedstrijden)||a.deelnemer.localeCompare(b.deelnemer));els.standBody.innerHTML=rows.map((row,index)=>`<tr><td>${e(String(index+1))}</td><td>${e(row.deelnemer)}</td><td>${e(String(row.punten_wedstrijden??0))}</td><td>${e(String(row.punten_bonus??0))}</td><td>${e(String(row.totaal??0))}</td></tr>`).join('')}function renderMatches(){const grouped=groupBy(data.matches||[],m=>`${m.datum_iso||m.datum||'zonder-datum'}||${m.datum||'Onbekende datum'}||${m.fase||'Wedstrijd'}`);els.matchesContainer.innerHTML=Object.entries(grouped).map(([groupKey,groupMatches])=>{const[,dateLabel,phaseLabel]=groupKey.split('||');return `<section class="group"><div class="bar red group-header"><span>${e(dateLabel)}</span><span class="fase">${e(phaseLabel)}</span></div><div class="cards-grid">${groupMatches.map(renderMatchCard).join('')}</div></section>`}).join('')}function renderMatchCard(match){const result=match.uitslag||'–';const rows=(data.participants||[]).map(name=>{const prediction=match.predictions?.[name];const points=match.points?.[name]??0;const cls=prediction==null||prediction===''?'points-zero':(points>=10?'points-good':points>0?'points-mid':'points-zero');return `<tr><td>${e(name)}</td><td>${e(prediction||'—')}</td><td class="${cls}">${e(String(points||0))}</td></tr>`}).join('');return `<article class="card"><div class="card-shell"><div class="bar blue match-bar"><div class="match-item match-time">${e(match.tijd||'')}</div><div class="match-item match-home">${e(match.thuis)}</div><div class="match-item match-result">${e(match.uitslag||'–')}</div><div class="match-item match-away">${e(match.uit)}</div></div><table class="data-table"><thead><tr><th>Deelnemer</th><th>Voorspelling</th><th>Punten</th></tr></thead><tbody>${rows}</tbody></table></div></article>`}function renderBonus(){els.bonusContainer.innerHTML=`<section class="group"><div class="bar red group-header"><span>Bonusvragen</span></div><div class="cards-grid">${(data.bonus_questions||[]).map(renderBonusCard).join('')}</div></section>`}function renderBonusCard(question){const rows=(data.participants||[]).map(name=>{const answer=question.antwoorden?.[name]??'—';const points=question.punten?.[name]??0;const cls=points>=10?'points-good':points>0?'points-mid':'points-zero';return `<tr><td>${e(name)}</td><td>${e(String(answer))}</td><td class="${cls}">${e(String(points))}</td></tr>`}).join('');return `<article class="card"><div class="card-shell"><div class="bar blue bonus-question-bar">Bonusvraag ${e(String(question.nr||''))}: ${e(question.vraag||'')}</div><div class="correct-answer">Correct antwoord: <strong>${e(question.correct_antwoord||'nog niet ingevuld')}</strong></div><table class="data-table"><thead><tr><th>Deelnemer</th><th>Antwoord</th><th>Punten</th></tr></thead><tbody>${rows}</tbody></table></div></article>`}function groupBy(items,keyFn){return items.reduce((acc,item)=>{const key=keyFn(item);(acc[key]||=[]).push(item);return acc},{})}function e(v){return String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;')}init();

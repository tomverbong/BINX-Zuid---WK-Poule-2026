const data = window.POULE_DATA || {}; const els = { matchesContainer: document.getElementById('matchesContainer'), bonusContainer: document.getElementById('bonusContainer'), standBody: document.querySelector('#standTable tbody'), tdfBody: document.querySelector('#tdfTable tbody'), tdfMeta: document.getElementById('tdfMeta'), matchesView: document.getElementById('matchesView'), bonusView: document.getElementById('bonusView') }; function init() { renderStand(); renderMatches(); renderBonus(); renderTdfStand(); bindToggle(); bindPouleSwitch(); setTimeout(scrollToToday, 150); } function bindPouleSwitch(){ document.querySelectorAll('.poule-btn').forEach(btn=>{ btn.addEventListener('click',()=>{ const view=btn.dataset.poule; document.querySelectorAll('.poule-btn').forEach(b=>b.classList.toggle('active', b===btn)); document.getElementById('wkPoule').classList.toggle('active-poule', view==='wk'); document.getElementById('tdfPoule').classList.toggle('active-poule', view==='tdf'); }); }); } function bindToggle() { document.querySelectorAll('.toggle-btn').forEach(btn => { btn.addEventListener('click', () => { const view = btn.dataset.view; document.querySelectorAll('.toggle-btn').forEach(b => b.classList.toggle('active', b === btn) ); els.matchesView.classList.toggle('active-view', view === 'matches'); els.bonusView.classList.toggle('active-view', view === 'bonus'); }); }); } function renderStand() { const rows = [...(data.stand || [])].sort( (a,b)=>Number(b.totaal)-Number(a.totaal)||Number(b.punten_wedstrijden)-Number(a.punten_wedstrijden)||a.deelnemer.localeCompare(b.deelnemer)); els.standBody.innerHTML = rows.map((row,index)=>`<tr><td>${e(String(index+1))}</td><td>${e(row.deelnemer)}</td><td>${e(String(row.punten_wedstrijden??0))}</td><td>${e(String(row.punten_bonus??0))}</td><td>${e(String(row.totaal??0))}</td></tr>`).join(''); } function renderTdfStand(){ const rows=[...(data.tdf_stand||[])].sort((a,b)=>Number(a.rang)-Number(b.rang)||Number(b.totaal)-Number(a.totaal)||a.deelnemer.localeCompare(b.deelnemer)); if(els.tdfBody){els.tdfBody.innerHTML=rows.map(row=>`<tr><td>${e(String(row.rang??''))}</td><td>${e(row.deelnemer??'')}</td><td>${e(String(row.totaal??0))}</td></tr>`).join('');}} function renderMatches(){ const grouped=groupBy(data.matches||[],m=>`${m.datum_iso||''}||${m.datum||'Onbekende datum'}||${m.fase||'Wedstrijd'}`); els.matchesContainer.innerHTML=Object.entries(grouped).map(([groupKey,groupMatches])=>{const [isoDate,dateLabel,phaseLabel]=groupKey.split('||');return `<section class="group" data-date="${e(isoDate)}"><div class="bar blue group-header"><span>${e(dateLabel)}</span><span class="fase">${e(phaseLabel)}</span></div><div class="cards-grid">${groupMatches.map(renderMatchCard).join('')}</div></section>`;}).join(''); } function renderMatchCard(match){ const result=match.uitslag||'–'; const rows=(data.participants||[]).map(name=>{const prediction=match.predictions?.[name]; const points=match.points?.[name]??0; const cls=prediction==null||prediction===''?'points-zero':points>=10?'points-good':points>0?'points-mid':'points-zero'; return `<tr><td>${e(name)}</td><td>${e(prediction||'—')}</td><td class="${cls}">${e(String(points||0))}</td></tr>`;}).join(''); return `<article class="card-shell"><div class="match-bar"><div class="match-item bar blue">${e(match.tijd||'')}</div><div class="match-item bar blue match-home">${e(match.thuis)}</div><div class="match-item bar red">${e(result)}</div><div class="match-item bar blue match-away">${e(match.uit)}</div></div><table class="data-table"><thead><tr><th>Deelnemer</th><th>Voorspelling</th><th>Punten</th></tr></thead><tbody>${rows}</tbody></table></article>`;} function renderBonus(){els.bonusContainer.innerHTML=`<section class="group"><div class="bar blue">Bonusvragen</div><div class="cards-grid">${(data.bonus_questions||[]).map(renderBonusCard).join('')}</div></section>`;} function renderBonusCard(question){ const rows=(data.participants||[]).map(name=>{const answer=question.antwoorden?.[name]??'—'; const points=question.punten?.[name]??0; const cls=points>=10?'points-good':points>0?'points-mid':'points-zero'; return `<tr><td>${e(name)}</td><td>${e(String(answer))}</td><td class="${cls}">${e(String(points))}</td></tr>`;}).join(''); return `<article class="card-shell"><div class="bonus-question-bar bar blue">Bonusvraag ${e(String(question.nr||''))}: ${e(question.vraag||'')}</div><div class="correct-answer">Correct antwoord: ${e(question.correct_antwoord||'nog niet ingevuld')}</div><table class="data-table"><thead><tr><th>Deelnemer</th><th>Antwoord</th><th>Punten</th></tr></thead><tbody>${rows}</tbody></table></article>`;} function scrollToToday(){ const today=new Date().toISOString().split('T')[0]; const groups=[...document.querySelectorAll('.group[data-date]')]; let target=null; for(const group of groups){const date=group.getAttribute('data-date'); if(date&&date>=today){target=group;break;}} if(!target&&groups.length){target=groups[groups.length-1];} if(target&&document.getElementById('wkPoule')?.classList.contains('active-poule')){target.scrollIntoView({behavior:'smooth',block:'start'});} } function groupBy(items,keyFn){return items.reduce((acc,item)=>{const key=keyFn(item);(acc[key]||=[]).push(item);return acc;},{});} function e(v){return String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');} init();